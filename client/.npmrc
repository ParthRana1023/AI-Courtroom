# Allow sharp's native build scripts to run (image processing for Next.js)
side-effects-cache=false
shamefully-hoist=true

# Approve build scripts for specific packages
onlyBuiltDependencies=["sharp"]
